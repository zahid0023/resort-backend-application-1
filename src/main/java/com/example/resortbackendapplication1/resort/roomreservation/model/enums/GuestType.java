package com.example.resortbackendapplication1.resort.roomreservation.model.enums;

/** Maps 1:1 to the Postgres native enum {@code guest_type} (see V47). */
public enum GuestType {
    ADULT,
    CHILD
}
